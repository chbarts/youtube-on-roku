[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "youtube-on-roku"
version = "0.1"
license = "CC0-1.0"
license-files = ["LICENSE"]
dependencies = [
    "yt_dlp",
    "requests"
]

[project.scripts]
youtube-on-roku = "youtubeonroku:main"
